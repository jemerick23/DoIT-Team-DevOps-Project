pipeline {
    agent any

    stages 
        stage('Run Server') {
            steps {
                bat 'start /B node server.js'
            }
        }

        stage('Verify') {
            steps {
                bat 'curl http://localhost:3000'
            }
        }
    }
}
